{
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "scripts": {
    "format": "prettier --write '**/*.{js,ts}'",
    "push": "git push && clasp push"
  },
  "dependencies": {
    "@types/google-apps-script": "^1.0.92"
  }
}
